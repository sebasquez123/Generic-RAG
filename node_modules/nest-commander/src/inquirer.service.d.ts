import { DiscoveryService } from '@golevelup/nestjs-discovery';
import { Answers } from 'inquirer';
import * as inquirerImport from 'inquirer';
export declare class InquirerService {
    readonly inquirer: typeof inquirerImport;
    private readonly discoveryService;
    constructor(inquirer: typeof inquirerImport, discoveryService: DiscoveryService);
    private readonly inquirerKeyToMetadataKeyMap;
    ask: <T extends Answers>(questionSetName: string, options: Partial<T> | undefined) => Promise<T>;
    prompt<T extends Answers>(questionSetName: string, options: Partial<T> | undefined): Promise<T>;
    private findQuestionSet;
    private mapMetaQuestionToQuestion;
    private parseRawQuestionMetadata;
}
