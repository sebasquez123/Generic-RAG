import { Injectable } from '@nestjs/common';

@Injectable()
export class PdfIngestionAdapter {
  // Next step: implement PDF parsing with a focused parser dependency.
  // Keep parsing here, not in the RAG or vector-memory modules.
}
